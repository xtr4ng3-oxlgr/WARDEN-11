# Secrets Patrol

Secrets Patrol scans local files controlled by the user.

It looks for patterns such as:

- API keys,
- tokens,
- webhooks,
- private keys,
- password assignments.

Values are masked in reports.
